import { SafeAreaWrapper } from "@/hoc/SafeAreaWrapper";
import { View } from "react-native";

export default function SignIn() {
  return (
    <SafeAreaWrapper>
      <View>Log in page</View>
    </SafeAreaWrapper>
  );
}
